package ooga.model.enums.backend;

/**
 * all these fields should correspond to Int Value only.
 */
public enum PlayerPara {
    COLOR, //initialization set by control
    Game, //initialization set by control
    IMAGE, //initialization set by control
    DIFFICULTY, //initialization set by control
    LIFE, //initialization set by default data value
    SCORE_GOAL, //initialization set by default data value
    CURRENT_SCORE, //initialization set by default data value
    CURRENT_LEVEL; //initialization set by default data value
}
