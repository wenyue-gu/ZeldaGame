package ooga.model.enums;

/**
 * all these fields should correspond to Int Value only.
 */
public enum PlayerPara {
    COLOR,
    Game,
    IMAGE,
    DIFFICULTY,
    LIFE,
    SCORE_GOAL,
    CURRENT_SCORE,
    CURRENT_LEVEL;
}
